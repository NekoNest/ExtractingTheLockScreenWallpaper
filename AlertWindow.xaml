<Window x:Class="ExtraTheLockScreen.AlertWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="AlertWindow" 
        Height="135" Width="400" 
        ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Name="AlertMessage" Margin="5"></TextBlock>
        <Button Grid.Row="1" Grid.Column="0" Name="NegativeButton" Margin="5"></Button>
        <Button Grid.Row="1" Grid.Column="1" Name="PositiveButton" Margin="5"></Button>
    </Grid>
</Window>